# Perf Lab

This directory is reserved for auxiliary perf-lab helpers and notes.

The browser-backed lab now lives in `apps/perf-lab/`.

Run:

- `bun run perf:lab`

This directory can still hold non-UI perf utilities later, but it is no longer the primary lab entrypoint.
