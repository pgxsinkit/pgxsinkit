# PGlite filesystem operation inventory

This is the Phase 1 gate for format version 1. It inventories the pinned
`BaseFilesystem` surface before the closed transaction-record union is defined.
The source of truth is `@electric-sql/pglite/basefs` as provided by the pinned
`@pgxsinkit/pglite` fork.

The Emscripten adapter performs several translations that are part of the host
contract:

- `getattr` and `lookup` call `lstat`;
- `setattr` may call `chmod`, `truncate`, and `utimes`; the pinned host currently
  calls `truncate` twice when a size is present, so truncation must be idempotent;
- `mknod` calls `mkdir` for directories and `writeFile(path, "")` for files;
- stream `open` calls `open(path)` without forwarding flags or mode;
- duplicated streams share one descriptor and close it when the shared refcount
  reaches zero;
- `llseek(..., SEEK_END)` calls `fstat`;
- `mmap` reads through `read`, while `msync` writes through `write`;
- the host wrapper currently swallows `unlink` errors after recording them; and
- symbolic links are rejected by the host before reaching this package.

## Deep VFS surface

| Operation                                     | Classification                                                                  | Creates extents                | Retires extents                          | Orphan-capable                                                                                                 | Partial success                                                                                                                                              | Transaction record                                                                       | Required semantics                                                                                                                                                                                     |
| --------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------ | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `chmod(path, mode)`                           | metadata-only                                                                   | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | yes                                                                                      | Preserve mode `0`; reject invalid mode/path before mutation.                                                                                                                                           |
| `close(fd)`                                   | runtime-only                                                                    | no                             | no                                       | closes an orphan descriptor only                                                                               | no                                                                                                                                                           | no                                                                                       | Idempotent only through lifecycle cleanup; closing the last orphan descriptor does not change persistent allocation.                                                                                   |
| `fstat(fd)`                                   | read-only                                                                       | no                             | no                                       | reads runtime orphan metadata                                                                                  | no                                                                                                                                                           | no                                                                                       | Reject an invalid descriptor with `EBADF`.                                                                                                                                                             |
| `lstat(path)`                                 | read-only                                                                       | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | no                                                                                       | Return exact inode metadata; do not follow links because links are unsupported.                                                                                                                        |
| `mkdir(path, options)`                        | metadata-only                                                                   | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | yes, one record for the full call                                                        | Recursive creation records every chosen inode ID, timestamp, and mode in one transaction; existing parents are not rewritten.                                                                          |
| `open(path, flags?, mode?)`                   | runtime-only, except caller-visible create/truncate flags when invoked directly | only for create/truncate forms | truncate forms may retire                | an existing inode may later become orphaned                                                                    | no                                                                                                                                                           | create/truncate forms use the corresponding create/resize record; ordinary open has none | Parse Node-style flags (`r`, `r+`, `w`, `w+`, `a`, `a+`, and exclusive variants); descriptor stores access/append semantics and an offset. The pinned Emscripten wrapper calls the existing-file form. |
| `readdir(path)`                               | read-only                                                                       | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | no                                                                                       | Directory only; return direct child names without exposing mutable maps.                                                                                                                               |
| `read(fd, buffer, offset, length, position)`  | data-only read                                                                  | no                             | no                                       | yes                                                                                                            | read may end at EOF                                                                                                                                          | no                                                                                       | Validate descriptor access, buffer slice, and position; explicit position does not advance the descriptor offset, while an omitted/internal offset path does.                                          |
| `rename(oldPath, newPath)`                    | metadata-only                                                                   | no                             | replacement may retire                   | replacing an open destination creates an orphan; moving an open source keeps descriptors attached to its inode | no                                                                                                                                                           | yes                                                                                      | Atomic replacement-by-rename; derive released destination extents in the reducer. Reject directory cycles and incompatible replacement types before mutation.                                          |
| `rmdir(path)`                                 | metadata-only                                                                   | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | yes                                                                                      | Directory must exist, be empty, and not be root.                                                                                                                                                       |
| `truncate(path, length)`                      | mixed                                                                           | extension may allocate         | shrink may retire whole trailing extents | path form cannot target an unlinked inode                                                                      | extension data preparation may fail before commit; operation itself is all-or-nothing                                                                        | yes when size or allocation changes                                                      | Shrink never zeroes. Extension zeros the whole newly visible gap and durably flushes gap bytes in pre-existing arena storage before the size record.                                                   |
| `unlink(path)`                                | metadata-only                                                                   | no                             | yes, into quarantine                     | unlinking an open file creates runtime orphan descriptors                                                      | no                                                                                                                                                           | yes                                                                                      | Remove entry and persistent inode together; reducer derives retired extents.                                                                                                                           |
| `utimes(path, atime, mtime)`                  | metadata-only                                                                   | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | yes                                                                                      | Store both exact validated timestamps; replay never consults the clock.                                                                                                                                |
| `writeFile(path, data, options)`              | mixed                                                                           | create/extend may allocate     | truncating flags may retire              | replacing/truncating an open file keeps its inode and descriptors                                              | underlying write may return a positive prefix internally, but the public `void` call must either complete or surface the failure according to host semantics | yes                                                                                      | Honor encoding, mode, and flag; create choices and the successfully written size/extents are one transaction. No hidden multi-frame call.                                                              |
| `write(fd, buffer, offset, length, position)` | mixed for extension, data-only within visible size                              | extension may allocate         | no                                       | yes; orphan growth reserves new extents in quarantine                                                          | yes: return the exact positive prefix; zero-byte failure throws                                                                                              | yes only when persistent metadata changes, or `reserveQuarantine` for orphan growth      | Validate access and bounds first. Existing visible bytes may be written in place. Extension commits only the written prefix. Append descriptors ignore the supplied logical position for placement.    |
| `strictSync()`                                | durability boundary                                                             | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | no                                                                                       | Flush dirty arena first, then active metadata; a successful return stabilizes all preceding acknowledged work.                                                                                         |
| `syncToFs(hostRelaxedDurability)`             | lifecycle/durability assertion                                                  | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | no                                                                                       | Assert the host argument agrees with construction. Strict mode delegates to strict sync; relaxed mode checks health and runs/queues due deferred repack work without an ordinary strict flush.         |
| `initialSyncFs()`                             | lifecycle/recovery                                                              | no                             | no                                       | no                                                                                                             | no                                                                                                                                                           | no                                                                                       | Acquisition, bootstrap, or staged recovery; publish live state once only after complete validation.                                                                                                    |
| `dumpTar(dbname, compression)`                | host helper over the mounted filesystem                                         | no                             | no                                       | no                                                                                                             | read may fail                                                                                                                                                | no                                                                                       | Inherited host traversal observes this VFS only through its normal operation seam.                                                                                                                     |
| `closeFs()`                                   | lifecycle/durability/cleanup                                                    | no                             | no                                       | closes all runtime descriptors                                                                                 | cleanup aggregates failures                                                                                                                                  | no                                                                                       | From OPEN, force strict ordering then attempt every cleanup. From FAILED, perform no persistence and preserve the first terminal cause.                                                                |
| `cleanupFailedInit()`                         | cleanup-only                                                                    | no                             | no                                       | closes all acquired resources                                                                                  | cleanup aggregates failures                                                                                                                                  | no                                                                                       | Idempotently close every handle acquired before initialization failed; never attempt persistence.                                                                                                      |

## Closed record-bearing operation set

The inventory requires records for these semantic transitions:

1. create one or more directories;
2. create a file;
3. change mode;
4. change timestamps;
5. resize a linked file;
6. commit an extending linked-file write;
7. remove a file;
8. remove an empty directory;
9. rename, optionally replacing a destination;
10. reserve additional quarantined extents for a growing orphan.

Ordinary in-range data writes, reads, stats, directory reads, descriptor creation,
descriptor duplication/closure, and durability/lifecycle operations do not carry a
transaction record. State-derivable release sets are deliberately absent from the
record set: the reducer derives them from the staged inode graph.

Every record-bearing transition is one frame. If its exact frame or projected base
cannot fit format-version-1 writer limits, planning rejects before persistent data
work; extending writes instead cap the admitted prefix where POSIX short-write
semantics apply.
